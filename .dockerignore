# Node.js 의존성 폴더 (RUN yarn install 에서 다시 생성되므로 빌드 컨텍스트에 포함 불필요)
node_modules/

# Git 관련 파일 및 폴더
.git/
.gitignore

# macOS 관련 파일
.DS_Store

# 개발 환경에서 생성되는 임시 파일이나 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE 관련 파일 (VS Code)
.vscode/

# Vite 빌드 결과물 (빌드 스테이지에서 생성되므로, 소스 코드 복사 시 포함되면 안 됨)
dist/

# gitpod 관련 파일 (개발 환경에만 필요)
.gitpod.yml
